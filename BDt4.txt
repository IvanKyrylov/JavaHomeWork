SELECT Concat(name,' ', surname, ' ', phone)
FROM *(дописать бд).staff;

SELECT Concat_ws(' ', name, surname, phone)
FROM *.staff;

SELECT Concat(surname, ' ', name, ';') AS 'Співр. з. вищ. осв.' 
FROM *.staff
WHERE staff.education = 'Вища'
ORDLES OR ****(тестить хз как работает);

SELECT Concat(surname, ' ', name, '-', rate) AS 'Ставка співробітника'
FROM *.staff
ORDLES OR staff.education DEST;
